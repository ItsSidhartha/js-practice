const number = 5;
let multiplier = 1;
const finalMultiplier = 10;

while(multiplier <= finalMultiplier){
    console.log(number, '*', multiplier, '=', number * multiplier);
    multiplier = multiplier + 1;
}
