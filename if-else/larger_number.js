const firstInput = 23;
const secondInput = 32;

if (firstInput < secondInput) {
    console.log(firstInput, 'is less than',secondInput);
} else if (secondInput > firstInput) {
    console.log(firstInput, 'is not less than', secondInput);
} else {
    console.log(firstInput, 'is equal to', secondInput);
}
 
